Master Setup Guide
==================

Last updated: Validation orchestrator now logs each issue separately with severity-specific console output and a compact summary line

FSM event flow semantics (authoritative):

Definition-driven FSM runtime (authoritative):
- `UnitBrain` now boots `StateMachineRuntime` and resolves a machine from:
  1) directly assigned `MachineDefinition`, or
  2) `MachineDefinitionId` through `StateMachineRegistry`.
- Runtime state lookup is keyed by `StateDefinition` IDs declared in `StateMachineDefinition.States`.
- Runtime transition lookup is keyed by `fromStateId` and evaluates transition eligibility using:
  - `eventName` / legacy `conditionDescription` event token,
  - optional `conditionId` (resolved through `ConditionRegistry`),
  - optional `minElapsed` token from legacy description parsing.
- `StateMachineDefinition.InitialStateId` is preferred for start state selection.

FSM transition asset generation semantics (editor automation):
- Editor utility: `Tools/FSM/Generate Transition Assets from Definitions`.
- Source of truth: `StateMachineDefinition.transitions` (`fromStateId`, `toStateId`, `eventName`, `conditionId`, `conditionDescription`).
- Generated asset path format: `Assets/FSM/Transitions/<machineId>/<from>_to_<to>__<stableHash>.asset`.
- Stable key format includes machine + from + to + event/condition/payload tokens so reruns are idempotent.
- Generator updates existing auto-generated assets, creates missing ones, and deletes stale generated assets that are no longer represented in definitions.
- Auto-generated transitions are marked with:
  - label `FSM-AutoGeneratedTransition`,
  - hidden metadata fields on `BehaviourTransition` (`isAutoGenerated`, machine/from/to IDs, condition/payload, stable key, comment).
- Automatic synchronization triggers on domain reload and when `StateMachineDefinition` assets are imported/moved/deleted.

FSM hierarchical transition lifecycle semantics:
- `UnitBrain` still computes old/new ancestry chains and performs LCA enter/exit traversal.
- Parent ancestry is now sourced from definition data when available:
  - Runtime checks `StateDefinition.parentStateId` first.
  - If no definition parent mapping exists, runtime falls back to legacy `BehaviourState.Parent`.
- During transition, `OnExit` is called from old leaf upward (excluding LCA), then `OnEnter` from below LCA down to new leaf.

Migration and deprecation notes:
- `UnitBrain.LegacyStateMappings` maps legacy `BehaviourState` assets to `StateDefinition` IDs while content migrates.
- `UnitBrain.LegacyInitialState` is a temporary fallback if no initial state ID can be resolved.
- Legacy direct wiring fields (`InitialState`, `Transitions`) remain only as deprecated compatibility fallback.
- `BehaviourTransition` assets are not part of the authoritative runtime path; generated copies exist for migration visibility and tooling workflows.

Legacy validation architecture notes:
- Unity menu: `Tools/Validation/Validate All Definitions`
- Runtime bootstrap, editor menu validation, and CI use the same validation orchestration core.
- CI execute method: `DefinitionValidationMenu.ValidateAllForCI`.


Validation logging semantics (authoritative):
- `DefinitionValidationOrchestrator.RunValidationAndLog()` now emits one log line per validation issue for direct console triage.
- Severity routing is explicit:
  - `ValidationIssueSeverity.Error` -> `Debug.LogError`
  - `ValidationIssueSeverity.Warning` -> `Debug.LogWarning`
  - `ValidationIssueSeverity.Info` -> `Debug.Log`
- Each issue log line includes: validation code, registry, message, and optional asset metadata (`AssetPath`, `AssetId`, `Field`) when present.
- The run ends with a compact summary line in the format: `[Validation] N issue(s) / M error(s).`
- Editor and CI entry points continue using `report.HasErrors` / `report.ErrorCount` for pass/fail behavior.
